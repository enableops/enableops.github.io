---
title: enableOps 101
---

import YouTube from "react-youtube";

Now once you are done with

## GitOps principle

Every infrastructure created by enableOps service is declared as Code. So the only way to change something in infrastructure (e.g. roll out new version of the app, add new testing cluster) is to update the code in one of infrastructure repositories that were created during initializing phase. And for a little bit more info on that topic you can watch this video:

<YouTube videoId="f5EpcWp0THw" opts={{ width: "100%", height: "400" }} />

## Building blocks

### Clusters

### Apps
